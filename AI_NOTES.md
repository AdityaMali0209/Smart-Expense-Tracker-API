# AI Notes

## 1. Which parts of the code were AI-generated vs. written by you
[TODO: Fill this in. Example: "Most of the boilerplate for Express and the Jest test suite was AI-generated. I wrote the logic for the specific bonus feature manually."]

## 2. What you validated, tested, or changed in the AI's output, and why
[TODO: Fill this in. Example: "I ran the test suite and noticed that the date formatting in the summary endpoint didn't handle single-digit months correctly, so I modified the string padding logic to ensure formats like 'YYYY-MM'."]

## 3. Any AI suggestion you decided not to use, and why
[TODO: Fill this in. Example: "The AI suggested using an SQLite in-memory database, but I decided to stick with a local JSON file as it was simpler and met the requirement that no database is needed."]
